{
  "name": "lynx-simple-server",
  "version": "1.0.0",
  "description": "Simple server for testing Agent 1 WIA with real wallet addresses",
  "main": "simple-server.js",
  "scripts": {
    "start": "node simple-server.js",
    "dev": "nodemon simple-server.js",
    "install:deps": "npm install && cd backend && npm install && cd ../agents/agent1-wia && npm install && cd ../intelligent-agent && npm install",
    "start:backend": "cd backend && npm run consolidated"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.0",
    "express": "^4.18.0",
    "tsx": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "keywords": [
    "blockchain",
    "wallet",
    "analysis",
    "agent"
  ],
  "author": "LYNX Team",
  "license": "MIT"
}
